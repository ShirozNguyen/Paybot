// PayBot v5.5.5 Part 66 — project Gradle HOÀN TOÀN ĐỘC LẬP cho neoforge-1.21.11 (cùng mô hình Part 63/64/65).
// Lý do tách: build thật xác nhận lỗi "Unfixable conflicts" khi remap minecraft qua TinyRemapper
// — nguyên nhân gốc do dev.architectury.loom 1.7.435 (root) quá cũ để xử lý đúng mapping/conflict
// cho NeoForge 1.21.11 (NeoForge dùng mojmap ở khắp nơi, hợp nhất phương thức giữa các class —
// gây xung đột intermediary mà arch-loom cũ chưa xử lý được; xác nhận qua GitHub issue
// architectury/architectury-loom#206). Dùng dev.architectury.loom 1.14.476 (bản mới hơn, xác
// nhận qua project mẫu Fuzss/bettermodsbutton build thành công NeoForge cho MC 1.21.10 bằng
// ArchLoom 1.11-SNAPSHOT — issue #323). File này KHÔNG được include()/includeBuild() từ root —
// chỉ build bằng cách tự chạy './gradlew build' TỪ ĐÚNG THƯ MỤC NÀY (CI gọi 'cd neoforge-1.21.11 &&
// ./gradlew build'). Xem LOG.md Part 66.
pluginManagement {
    repositories {
        maven { url = 'https://maven.architectury.dev/' }
        maven { url = 'https://maven.neoforged.net/releases' }
        gradlePluginPortal()
    }
}

rootProject.name = 'neoforge-1.21.11'
