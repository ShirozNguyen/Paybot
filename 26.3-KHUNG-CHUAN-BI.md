# Khung chuẩn bị MC 26.3 — CHƯA DỰNG MODULE THẬT (v5.5.5, Part 51)

> File này KHÔNG phải 1 module Gradle — không có `include('fabric-26.3')` nào trong
> `settings.gradle`. Lý do: tạo module rỗng lúc chưa có gì thật để cấu hình (không version
> Fabric API/NeoForge/Forge nào tồn tại cho 26.3 tính tới 23/8/2026) chỉ tạo giả tạo, có thể
> gây nhầm lẫn lúc build thật. File này là NGƯỜI GHI NHỚ cho phiên làm việc kế tiếp.

## Trạng thái tính tới 23/8/2026 (lúc viết, Part 48 research)

- Minecraft 26.3 **CHƯA release chính thức** — đang snapshot (snapshot 7 ra 4/8/2026), dự kiến
  chính thức khoảng cuối tháng 9/2026.
- Chưa có NeoForged Porting Primer cho 26.2→26.3 (logic — bản đích chưa ra).
- Chưa có version Fabric API/Fabric Loom, NeoForge/ModDevGradle, hay Forge/ForgeGradle nào công
  bố riêng cho 26.3 (các trang tra cứu, ví dụ `files.minecraftforge.net`, chỉ có tới 26.2).

## Việc cần làm khi 26.3 chính thức release (checklist cho phiên sau)

1. Xác nhận ngày release chính thức thật (web search — đừng tin ETA "cuối tháng 9/2026" ở trên,
   có thể đã đổi).
2. Đọc NeoForged Porting Primer 26.2→26.3 nếu có (`docs.neoforged.net/primer/docs/26.3/` —
   kiểm tra URL này có tồn tại chưa, cấu trúc URL có thể đổi).
3. Xác nhận version Fabric API mới nhất cho 26.3 (theo mẫu `0.1xx.x+26.3` — xem CurseForge/
   Modrinth listing của `fabric-api`).
4. Xác nhận build NeoForge mới nhất cho 26.3 (`projects.neoforged.net/neoforged/neoforge`).
5. Xác nhận build Forge cổ điển cho 26.3 (`files.minecraftforge.net` — kiểm tra xem Forge có
   tiếp tục ra bản cho 26.3 hay không, đừng giả định dựa vào việc 26.1/26.2 có — mỗi bản có thể
   khác nhau).
6. Nếu không có gì đổi lớn về toolchain (nhiều khả năng — 26.1→26.2 không đổi gì về mặt build
   tool, chỉ đổi version), nhân bản 3 module `*-26.2` sang `*-26.3` theo đúng cách đã làm ở
   Part 51 (xem `LOG.md` Part 51 mục 51.1). Nếu CÓ đổi lớn (vd đổi lại cách handle mapping,
   đổi major version ForgeGradle/ModDevGradle/Fabric Loom), cần research lại từ đầu như đã làm
   ở Part 48 cho 26.1 — đừng giả định 26.3 giống hệt 26.1/26.2 chỉ vì 2 bản trước giống nhau.
7. Rà soát lại các điểm rủi ro đã xác định (SavedData/DimensionDataStorage, ItemStackTemplate,
   Items merged constants, Gui/Hud, Shears/Advancement/GameTest) — 26.3 CÓ THỂ có thêm breaking
   change mới không nằm trong danh sách này, đọc primer 26.3 đầy đủ trước khi kết luận an toàn.
8. Cập nhật `settings.gradle`: thêm `include('fabric-26.3')` v.v. vào đúng vị trí (sau nhóm
   26.2 hiện có).
